name: 04 - Using Actions

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node
        run: |
          git clone git@github.com:gitbit787/github-actions-course.git
          ls -ila 
