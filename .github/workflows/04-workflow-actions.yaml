name: 04 - Using Actions

on:
  push:

jobs:
  build:
    run-on: ubuntu latest
    steps:
      - name: check out
        run: git clone git@github.com:gitbit787/github-actions-course.git
    steps:
      - name: printing folders
        run: pwd
     
