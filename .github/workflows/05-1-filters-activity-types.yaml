name: 05 - 1 - Event Filters and Activity

on: 
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  echo:
    run-on: ubuntu-latest
    steps:
      - name: print statement
        run: echo "running job only when PR and branch is main, when synchronized or opened type"


